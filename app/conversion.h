#ifndef CONVERSION_H
#define CONVERSION_H

	#include <stdio.h>
	#include <string.h>
	#include <stdlib.h>

	char * conversion(int s);

	char * millier(  int m);
	char * centaine( int c);
	char * dizaine(	 int d);
	char * unite(	 int u);
#endif
